services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: civic_nexus
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - civic_pg:/var/lib/postgresql/data

volumes:
  civic_pg:
